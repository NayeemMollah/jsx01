
const Button = ({ textbtn, className }) => {
    return (



        <>
          <button className={`${className}`}>{textbtn}</button>
        </>
          
       
    )
}

export default Button